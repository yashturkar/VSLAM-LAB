DATASET:
  # Directory containing either <name>/{rgb_0,rgb.csv,calibration.yaml,groundtruth.csv}
  # or the raw LIGHTNING image_0/, sequences/, poses/, and config.yaml layout.
  base_path: /path/to/lightning/sequence-root
  name: sequence_name
  baseline: mast3rslam
  dataset: lightning
  sensor_type: mono
  groundtruth_format: kitti
  output_dir: output
