DATASET:
  # Directory containing either <name>/{rgb_0,rgb.csv,calibration.yaml,groundtruth.csv}
  # or the raw LIGHTNING sequences/<name>/image_0, poses/, and shared lightning.yaml layout.
  base_path: /mnt/share/local/eph/clid-dataset/davis-lightning-12-2025/Sequences/Processed/113through_p0.5_extract
  name: 113through_p0.5_extract
  baseline: mast3rslam
  dataset: lightning
  sensor_type: mono
  groundtruth_format: kitti
  output_dir: output
